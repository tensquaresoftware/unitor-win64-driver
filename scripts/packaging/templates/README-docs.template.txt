Unitor MT4 Bridge {{VERSION}} — documentation package
=====================================================

Download the Setup that matches your Windows edition from the GitHub Release:

- `unitor-mt4-bridge-{{ARTIFACT_VERSION}}-win11-wms-setup.exe` — Windows 11 + Windows MIDI Services (simpler install; no virtualMIDI prerequisite)
- `unitor-mt4-bridge-{{ARTIFACT_VERSION}}-win10-virtualmidi-setup.exe` — Windows 10 + user-installed virtualMIDI (more technical; never embeds teVirtualMIDI.dll / MSI)
- This zip: manuals + README router + shared honesty pages (`license-and-backends.md`, `authenticode-and-smartscreen.md`)

Start here: open `README.md` in this zip (or on GitHub under docs/user/).
